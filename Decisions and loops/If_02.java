// The if-else statement

public class If_02 {
  public static void main(String[] args) {
    String name = "John";
    boolean ready = true;

    // The compiler can't define whether name is true or false
    // if (name) { System.out.println(name); }

    if (ready) { System.out.println("Hello"); }
  }
}
